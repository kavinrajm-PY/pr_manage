// src/app/lead/projects/[projectId]/page.tsx
// Server shell for Team Lead project details.

import LeadProjectDetailClient from './LeadProjectDetailClient';

export default function Page() {
  return <LeadProjectDetailClient />;
}
